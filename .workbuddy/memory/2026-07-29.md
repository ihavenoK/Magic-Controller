## 项目规则写入

- 从《嵌入式C项目开发规范手册》v3.0 DOCX 提取结构化规则
- 写入 `.workbuddy/rules/embedded-c-standards.md`（13 章 + 附录 A，~250 行）
- 每节标注 CyberryPotter 适用性：🔴强制 / 🟡建议 / ⚪不适用
- 同步更新 MEMORY.md 项目笔记

## 代码审查 + 修复

- 审查 main.c/CyberryPotter.c/IMU.c/hardware.c/IIC.c/Delay.c/config.h
- 修复 I2C 读取无错误处理（IMU.c:57,66）
- 修复 IIC_READ_SCL_PIN 命名错误 → IIC_READ_SDA_PIN（IIC.c:19,164）

## 红外模块接入

- 新增 `User/IR.h` + `User/IR.c`：NEC 协议发射（TIM2_CH1 PA15 38kHz PWM）+ 接收解码（TIM3_CH3 PB0 输入捕获）+ Flash 存读
- 修改 `config.h`：加 IR 引脚/Flash 地址宏
- 修改 `CyberryPotter.h`：eSystem_Mode 改为两态（GESTURE_IR/IR_LEARN），加 eSignal_Status/eSignal_Type
- 修改 `hardware.h/c`：加 IR_CARRIER_ON/OFF 宏，Hardware_Init 末尾调 IR_Init()
- 修改 `CyberryPotter.c`：System_Init 初始化 IR 字段，System_Status_Update 简化为两态切换
- 修改 `main.c`：CNN 识别后调用 IR_Send_NEC()，学习模式交互逻辑

## RAW 万能学习模式改造

- 放弃 NEC 协议解码 → RAW 脉冲录制（TIM3 ISR 记录每个下降沿时间差）
- Flash 存读改为 `IR_Raw_Signal_t`（edge_count + edges[200]）
- 发射改为 IR_Send_Raw() 逐脉冲回放 PWM 载波
- 诊断从 ISR 搬到主循环，加 5 秒 listening 提示
