## B1+B2 修复 + 统一改用加速度

- 修复 `config.h:60`: `IMU_GYRO_TRANS_RADIAN_CONSTANT` 8192.0 → 3755.0
- 修复 `main.c:39-41`: `model_feed_data()` 从 `IMU_Data_mGyro` 改为 `IMU_Data_mAcc`，注释同步修正
- 修改 `CNNTrainRaw.py:22`: `DEF_USE_COLS` 从 `(3,4,5)` 改为 `(0,1,2)`
- **需重新训练**: 旧 weights.h 模型基于陀螺仪数据，下一步需重新采集加速度训练数据并生成新模型
